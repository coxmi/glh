{
  "name": "glh",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "screenshots": "node --require ts-node/register test/common/playwright.ts --update-screenshots",
    "test": "node --require ts-node/register --test '**/*.test.ts'"
  },
  "dependencies": {
    "esbuild": "^0.27.2"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@types/pngjs": "^6.0.5",
    "glob": "^13.0.6",
    "pixelmatch": "^7.1.0",
    "playwright": "^1.58.2",
    "pngjs": "^7.0.0",
    "ts-node": "^10.9.2"
  }
}
